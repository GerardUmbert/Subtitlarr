<?xml version="1.0"?>
<Container version="2">
  <Name>Subtitlarr</Name>
  <Repository>ghcr.io/gerardumbert/subtitlarr:latest</Repository>
  <Registry>https://ghcr.io/gerardumbert/subtitlarr</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/GerardUmbert/Subtitlarr/issues</Support>
  <Project>https://github.com/GerardUmbert/Subtitlarr</Project>
  <License>https://github.com/GerardUmbert/Subtitlarr/blob/master/LICENSE</License>
  <Overview>UPDATE NOTICE (existing installs): this template now includes a new "MCP Server Port" (7778), used for optional AI-assistant access (e.g. Claude Code) — Docker/Unraid never adds new ports to an already-running container automatically. If you installed Subtitlarr before this notice appeared, either click Edit on your existing container and Apply once the new port field appears below, or manually add a Port mapping for 7778 -> 7778 yourself. New installs get this automatically and can ignore this notice.&#13;&#10;&#13;&#10;Fills gaps in your Bazarr subtitle library by translating subtitles you already have (e.g. English) into languages you're missing (e.g. Spanish), using a local or cloud LLM. Never mounts your media folders — all reading and writing of subtitles happens through Bazarr's own REST API, so Bazarr remains the only thing with filesystem access to your library.&#13;&#10;&#13;&#10;Everything except the port and data path below is configured from the web UI after first start — Bazarr connection, translation engines (Ollama, llama.cpp, Gemini, NVIDIA NIM, OpenRouter, Groq), scheduling, and all other settings persist in Subtitlarr's own database, not container environment variables.&#13;&#10;&#13;&#10;No media path needs to be mapped into this container — only the /data path below, which is Subtitlarr's own small SQLite database, not your library.</Overview>
  <Category>MediaApp:Other MediaApp:Video</Category>
  <WebUI>http://[IP]:[PORT:7777]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/GerardUmbert/Subtitlarr/master/unraid/subtitlarr.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/GerardUmbert/Subtitlarr/master/unraid/icon.png</Icon>
  <ExtraParams>--memory=2g --memory-swap=2g --restart=unless-stopped --log-opt max-size=10m --log-opt max-file=3</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires/>

  <Config Name="WebUI Port" Target="7777" Default="7777" Mode="tcp" Description="Host port mapped to the container's internal port 7777 — keep this matching 7777 on both sides. Unraid's built-in Tailscale hook auto-configures Serve to proxy to the HOST-mapped port number, so a mismatched mapping (e.g. a different host port than the container's actual internal port) breaks the Tailscale hostname URL even though local/LAN access still works fine." Type="Port" Display="always" Required="true" Mask="false">7777</Config>

  <Config Name="MCP Server Port" Target="7778" Default="7778" Mode="tcp" Description="Lets an MCP-aware AI assistant (e.g. Claude Code) connect remotely to monitor queue/job status and trigger translations, secured with a bearer token shown on the Jobs page. Set MCP_ENABLED (advanced, below) to false to disable the MCP server entirely instead of just hiding this port." Type="Port" Display="always" Required="true" Mask="false">7778</Config>

  <Config Name="Data" Target="/data" Default="/mnt/user/appdata/subtitlarr" Mode="rw" Description="Subtitlarr's own SQLite database (queue, run history, settings, engine instances). NOT a media path — do not point this at your library." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/subtitlarr</Config>

  <Config Name="PUID" Target="PUID" Default="99" Mode="" Description="User ID the app runs as inside the container — must own (or have write access to) the Data path above. Unraid's default 'nobody' user is 99." Type="Variable" Display="advanced" Required="false" Mask="false">99</Config>
  <Config Name="PGID" Target="PGID" Default="100" Mode="" Description="Group ID the app runs as inside the container. Unraid's default 'users' group is 100." Type="Variable" Display="advanced" Required="false" Mask="false">100</Config>
  <Config Name="MCP_ENABLED" Target="MCP_ENABLED" Default="true" Mode="" Description="Set to false to disable the MCP server process entirely (no listener on the MCP Server Port at all), if you don't intend to use AI-assistant access." Type="Variable" Display="advanced" Required="false" Mask="false">true</Config>
</Container>
